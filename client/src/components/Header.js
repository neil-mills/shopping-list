import React from 'react'
import LoginButton from './LoginButton';

const Header = () => {
  return (
    <header>
      <nav>Nav here</nav>
      <LoginButton />
    </header>
  )
}

export default Header;